<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="kr.campus.mapper.AdminMapper">
  
  <insert id="itemsEnroll">
     
        insert into tbl_items(itemCode, itemName, price, quantity, ratings, category, purchases, reviewno, qnano, thumbNail)
      values(#{itemCode},#{itemName}, #{price},#{quantity},#{ratings},#{category},#{purchases},#{reviewno},#{qnano},#{thumbNail})

     </insert>
  
  </mapper>